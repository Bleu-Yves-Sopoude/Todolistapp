<h1>Edit Task</h1>

<%= form_with(model: @task, local: true) do |form| %>
  <p>Name: <%= form.text_field :name %></p>
  <p>Description: <%= form.text_area :description %></p>
  <p>status: <%= form.check_box :name %></p>
  <p><%= form.submit %></p>
<% end %>

<%= link_to "Back", tasks_path %>